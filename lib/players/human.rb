module Players
  class Human < Player
  
    def move(x)
      # takes in a board argument and allows a human player to ender a move via the CLI (does not need to use it)
      #returns the value the user enters
    end
    
  end
end
# * need to call Human class with Players::Human *